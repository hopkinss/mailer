Failed to deploy to Heroku  - i couldnt get any of the instructions to work

Optional features
1) Create donor
 - If donor exists then add donation, otherwise create new donor
 - Added validation fields must not be null and dontation must be >$10
2) Login/logout page that displays user name in base template. Must be logged in to create donors
3) Added page with drop down list of distinct users that displays donations for selected user onchange
4) added bar and pie chart visualizations using Chart.js
5) included bootstrap, fontawesome, and user-defined css to style html